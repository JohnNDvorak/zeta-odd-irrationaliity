# Tosi 2026 notes

- Tosi makes basic cellular integral decompositions explicit enough to serve as future regression targets.
- This first pass does not implement the Tosi regressions numerically.
- The repository keeps placeholder fixtures and blocked tests so those checks can be activated once the CAS layer exists.
